<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>왼쪽 카드 UI</title>
    <style>
        /* 전체 페이지 리셋 및 완전 하얀색 배경 설정 */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background-color: #ffffff;
            width: 100vw;
            height: 100vh;
            display: flex;
            align-items: center;
            padding: 40px; /* 화면 테두리와의 여백 */
        }

        /* 왼쪽 카드 UI 스타일 */
        .card {
            width: 350px;           /* 카드의 너비 (필요에 따라 조절 가능) */
            height: 80%;            /* 카드의 높이 */
            background-color: #ffffff;
            border-radius: 16px;    /* 모서리를 부드럽게 둥글게 */
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.08); /* 은은한 그림자로 배경과 구분 */
            border: 1px solid #f0f0f0; /* 아주 연한 테두리 (선택사항) */
            padding: 24px;          /* 카드 내부 여백 */
            display: flex;
            flex-direction: column;
            justify-content: flex-start;
        }

        /* 카드 내부 콘텐츠 영역 (예시용 텍스트) */
        .card-content {
            color: #333333;
            font-family: 'Malgun Gothic', sans-serif;
        }

        .card-title {
            font-size: 20px;
            font-weight: bold;
            margin-bottom: 12px;
        }

        .card-desc {
            font-size: 14px;
            color: #666666;
            line-height: 1.5;
        }
    </style>
</head>
<body>

    <!-- 왼쪽 카드 UI -->
    <div class="card">
        <div class="card-content">
            <h2 class="card-title">Card Menu</h2>
            <p class="card-desc">이곳은 왼쪽 카드 UI 영역입니다. 나머지 공간은 비워져 있습니다.</p>
        </div>
    </div>

</body>
</html>
